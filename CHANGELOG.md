# 更新记录

## 2026-09-05

### 网关策略实施、原生工具与监控

- 全部模型统一 Schema 保真，移除隐式递归清洗及 strict 降级；新增带备份、历史金额校验和幂等检查的策略迁移脚本。
- 使用共享执行器限制重试边界，首业务事件返回后不再重放；保持原 sticky 长期冷却规则，修复重复重试与流缺少终态的错误处理。
- 接入认证后的调用方上下文、三天闲置会话缓存和永久 gzip 原生交换归档；OpenRouter/Gemini 思考签名隔离并精确匹配，优先保留客户端字段。
- Gemini 原生路由使用共享转发器，增加原生 Interactions 入口；兼容 OpenAI、DeepSeek 官方 web_search、百炼 Qwen 和 Gemini 的原生工具及相关事件、引用、签名。
- 新增管理员 Playground、文档能力诊断、实际工具输出观察、请求中断、完整响应下载和阶段耗时；提供四项只读网关管理工具及 MCP Streamable HTTP。
- 新增调用方、会话、网关请求 ID、耗时和调用时价格快照，覆盖 SQLite 全部相关读写；历史金额不再启动重算，现价分析独立只读。
- Key 保存/重载失败明确报错，普通配置变更回滚，删除和禁用保持当前进程立即生效；自定义 tokenizer 的远程代码改为按来源开启。
- 图床保留三十分钟并限制上传大小；请求日志永久保留且新文件无损压缩，支持混合读取旧 JSON/gzip 日志。
- 监控可搜索调用方和会话，显示阶段耗时与价格快照；长文本点击时才渲染全文，详情异步响应按当前窗口过滤，活跃请求不重复重建相同 DOM。
- 验证：279 项测试通过，128 个 Python 文件、17 个 JavaScript 文件语法检查通过；隔离环境验证应用装配，真实数据库迁移确认历史金额不变。供应商调用使用模拟上游。

### 全项目审查与兼容性修复

- 修复未配置 API Key 时携带任意 Key 可绕过本机限制；补齐 Gemini 模型列表鉴权方式与访客白名单过滤，非法请求类型返回 400、未知模型返回 404。
- 补齐模型删除/排序和 JSONC 管理写入的事务锁；无效配置保留最后有效状态；串行化 Key 管理与重载，防止较早快照恢复已撤销 Key。
- 修复配置模式切换丢失修改、注释丢失、字符串解析错误、有效 0 被默认值覆盖和浅拷贝污染；前后端及独立图床统一支持字符串感知的 JSONC 解析。
- 模型编辑保留其他端点和未在表单展示的配置字段；编辑保存始终检查原模型存在。未带内容指纹的 JS/CSS 重新验证浏览器缓存。
- 监控日志只接受最新查询结果，标签切换不依赖全局 event，费用属性统一转义；CSV 增加真实 UTF-8 BOM、防公式文本处理和正确的币种列名。
- 修复模型历史 USD/CNY 记录直接相加的问题，按当前汇率转换后汇总；自动归档统计查询移到线程池。
- 提取 SSE JSON 解析器，支持多行 data、UTF-8/CRLF 分块和最后无空行事件，避免大事件反复复制累计字符串。
- Gemini GenerateContent 补齐工具签名回传、跨分块调用编号、文本与工具共存、完成原因、JSON Schema 与停止序列转换。
- Anthropic 双向转换增加 strict、并行工具控制、结构化输出和 effort 映射；思考配置只修改 output_config.effort，不再删除输出格式。
- Responses 原生请求使用独立副本；流缺少终态事件时报告失败，content_filter 在转换分支保持 incomplete 状态。
- 修复独立图床的 HTTP 错误状态、base64 校验、清除元数据时丢失格式、图片处理阻塞事件循环，以及独立依赖缺少 Pillow。
- 补录工作区已有的 schema_sanitizer 依赖，修正 README 初始化示例与结构说明，增加开发测试依赖。
- 增加全项目审查报告，核对 11 家供应商文档，集中记录待决定的兼容策略、重试、权限和功能扩展；提供未注册到网关的只读 LLM 工具样例。
- 测试在收集模块前切换到独立临时目录，避免导入网关模块时初始化业务目录；真实页面资产仍从仓库读取。
- 验证：隔离环境下 244 个测试通过；105 个 Python 文件、16 个 JavaScript 文件和 2 段内联脚本语法检查通过。供应商调用使用模拟传输，没有进行付费线上探测。

### 管理界面重构

- 调整管理面板的导航、留白、按钮、卡片和移动端布局；按使用偏好采用直角控件，增加侧栏宽度与间距，避免导航文字换行。
- 模型列表增加名称 / 模型 ID / 地址搜索、接口协议筛选、服务地址筛选、数量提示和无结果状态。设置入口常驻，复制、归档和删除放入更多操作。
- 批量全选只作用于筛选后可见的模型，显示所选数量和部分选中状态。筛选期间暂停拖动排序，防止误改隐藏模型的顺序。
- 模型编辑器改为六组导航：连接与身份、生成与思考、消息与兼容、重试与图片、计费与统计、自定义参数。固定保存栏，支持未保存提醒、关闭后恢复焦点、键盘导航及 Ctrl / Cmd + S。
- 保存期间防止重复提交；失败保留输入并允许重试。新增 / 复制时检查当前列表中的同名模型，避免意外覆盖。
- API Key 默认遮蔽，可逐行显示；列表不再将含 Key 的完整配置嵌入 HTML 属性。
- 将模型编辑代码按表单保存、选项配置、Key 管理和界面交互拆分，保留原有配置格式及 LMArena 兼容入口。

### 修复

- 重置表单时恢复初始值，避免新增模型沿用之前编辑的兼容开关、计费等状态。
- 保留价格、缓存价格、温度上限中的有效数值 0。
- 根据接口协议显示默认端点；切换协议时保留自定义端点，并只显示适用的透传选项。
- 修复空 Key 行导致余额查询结果与输入行错位，以及余额货币字段缺少 HTML 转义的问题。
- 修复会话失效消息参数顺序、归档失败产生未处理 Promise，以及并发刷新模型列表时旧响应覆盖新响应的问题。

### 验证

- 新增真实 Chromium 浏览器回归，使用隔离模拟 API，不读写真实模型配置或调用上游。
- 浏览器测试：`python -m pytest tests/test_admin_workspace.py -q`。依赖 `pytest`、`playwright`、`beautifulsoup4`，浏览器通过 `python -m playwright install chromium` 安装。
- 模型归档回归：`python -m pytest tests/test_model_archive.py -q`。
