# 更新记录

## [Unreleased]

### 2026-09-27：Codex 计价与首页刷新修复

- 补充 GPT-6 Sol 标准 API 估算价：每百万 Token 普通输入 $2、缓存输入 $0.20、缓存写入 $2.50、输出 $10；单次输入超过 272K 时，输入与缓存单价乘 2、输出单价乘 1.5。
- codex-auto-review 按用户指定的 GPT-6 Sol 单价和长上下文规则估算，保留原始模型名称；页面显示计价映射及各模型价格核对日期，统计和 CSV 均包含估算金额。
- 首页手动刷新同时更新概览与 Token 统计并跳过旧缓存；Codex 用量改为后台增量检查，自动补齐更新，避免等待全部历史日志重新解析。

### 2026-09-26：近期功能审查修复

- Gemini 签名按原始内容块及顺序恢复，覆盖正文、独立空文本、思考和媒体签名；重复工具调用只恢复原本携带签名的那一项。
- 历史指纹包含媒体、工具 ID 和内容顺序，缓存作用域补入 cachedContent 与 toolConfig，避免不同上下文误用签名。多候选独立记录，仅保存明确 STOP 的完整轮次。
- 保留思考缺失与首尾空白裁剪的兼容，拒绝改写思考和删除内部空白。签名缓存升级为 v2，旧版因缺失原始块位置不再使用，后续完整响应会建立新缓存。
- 监控展示保留 Gemini 工具调用和结果的结构化字段，修复 Interactions 字符串输入在系统提示存在时被遗漏。
- Playground 模型目录和 Tokenizer 来源分别更新页面，来源列表缓慢时仍可选择模型与编辑草稿。

### 2026-09-25：Gemini 原生思考签名自动回传

- 原生 Gemini 请求按已完成模型轮次的可见历史前缀保存带签名的思考与工具调用；客户端无需回传 thoughtSignature，桥接在下一轮自动匹配并回填；思考整体缺失或仅被裁剪首尾空白时按存储原文回填，被改写的思考不会获得签名。客户端已自带签名、系统提示或工具变化时都不注入，同一前缀出现不同签名按歧义停用。
- 监控面板的 Gemini 请求消息开始展示思维链与签名（含思维链 / 含思维链签名徽标）；回填数量记录在请求参数的 restored_thought_signatures。
- 新增 tests/test_gemini_history.py 与原生交换恢复回归测试；全量 415 项通过。

### 2026-09-10：监控检索与请求调试完善

- 日志支持按自然日筛选、重置筛选、暂停自动刷新和手动重试；合并相同的在途查询，取消过期请求，避免旧结果和错误覆盖当前筛选。
- 最近日志读取不再统计全表；分页计数与记录使用相同 SQLite 快照，模型筛选目录按需读取，列表只增加思维链和工具调用布尔标记。旧记录的标记保留未知，历史费用不变。
- SQLite 查询失败时使用已有的最近日志回退，并明确提示最多检索最近 1000 条；日期和分页参数统一校验，数据库与文件筛选共用逻辑。
- Playground 保留本页各端点的请求草稿和会话设置，插入工具时保留自定义函数、未知工具及所选工具参数；新增 JSON 排版、快捷发送和对应监控记录入口。耗时读取不再阻塞下一次请求，旧耗时结果不能覆盖新请求。
- 修复刷新能力时丢失未保存的 Tokenizer 来源编辑、来源接口失败连带阻止模型加载的问题；导航缩放不再对布局属性做动画，手机导航保持单行。
- 完整回归 387 项通过；日志交互最后的收尾修正另通过 11 项定向浏览器回归。桌面 1440px 和手机 390px 的监控与 Playground 截图检查通过。

### 监控新增功能审查与界面整理

- 修正缓存对比的结论分支：工具变化、历史缺失或截短时分别说明证据，不再直接断言缓存淘汰或共享同一上游缓存空间；响应消息不再被误报为请求参数变化，兼容旧版嵌套参数和参数重名记录。
- 修复消息字段顺序改变时同时显示“首个差异”和“严格追加”的矛盾状态，以及含 emoji 的消息差异标记偏移；工具信息仅一侧存在时显示无法确认，识别 Responses 原生 function 名称。
- 修复对比窗口的旧响应/旧错误覆盖、关闭后仍渲染、初始焦点和返回焦点；从详情加入第三条请求时同步取消被替换记录的勾选。请求 ID 改用数据属性传递并编码 URL，避免引号破坏事件处理。
- 速度统一说明为输出阶段平均速度和端到端速度；首业务事件不再误标成首 token 时间，非流式响应在详情与对比中均说明速度测量限制。大日志对比计算移到工作线程，避免占用异步事件循环。
- 监控页采用暗色平面、直角控件和蓝色强调；活跃请求与日志前置，新增独立对比栏、可移除的已选请求标识、跨页选择提示及行高亮，调整窄屏摘要、长字段和对比表格布局；将对比状态与渲染拆入独立模块。
- 验证：缓存对比后端 22 项测试、监控与管理界面 27 项真实 Chromium 测试通过；使用模拟日志核对桌面和手机截图，不调用真实供应商、不修改业务数据。

### 监控：请求详情 TPS 与双日志缓存对比

- 请求详情新增“生成速度”行：解码速度（输出 token / 输出阶段耗时）、端到端速度（输出 token / 总耗时）、TTFT，并对非流式响应的虚高解码速度给出提示。
- 请求日志表格支持勾选两条记录，对比缓存命中差异：缓存摘要（命中率、差值、块对齐、时间间隔）、归属（会话/调用方是否共享缓存空间）、消息首个差异定位（前缀敏感的断裂点高亮）、工具定义、请求参数与结论。
- 新增 `GET /api/monitor/compare?a=&b=` 结构化对比接口（按时间自动排旧新），对比逻辑收敛到 `utils/prompt_cache_compare.py`，`scripts/analyze_prompt_cache.py` 改为复用它的命令行包装（输出格式不变，另增归属段落；耗时/请求 ID 等每次必变字段不再误报为参数差异）。
- 新增 `tests/test_prompt_cache_compare.py` 对比与 TPS 回归测试。

## 2026-09-07

### 上游请求头透传

- 修复 Responses 原生透传和 Chat Completions 透传没有转发客户端端到端请求头的问题，包含 `x-opencode-session` 和自定义调用方标识头；客户端未提供时保持缺省。
- 仅排除代理控制、连接级、请求体重新编码后不能复用的头，以及桥接层本地鉴权和会话控制头。

## 2026-09-06

### Responses 流式错误详情保留

- 修复 Responses 转 Chat 时丢失顶层 `error` 事件的 `message`、`code`、`param` 的问题。上游提前结束、读取超时等错误现在保留实际原因，不再统一显示“上游 Responses 请求失败”；嵌套错误格式保持兼容。
- 增加流式转换回归测试，覆盖顶层错误、嵌套错误、失败终态和缺少错误详情的情况。

### 客户端裁剪空白时的推理回传修复

- 修复手机端裁剪助手回答开头空白导致历史前缀匹配失败的问题；仅在匹配指纹中统一助手正文首尾空白、CRLF/LF 和文本块表示，回传仍保留原始内容与完整推理项。正文内部空白、代码缩进、用户消息首尾空白仍准确匹配。
- 规范化后出现不同原始输出的前缀仍按歧义处理，避免宽容匹配选错签名。
- 使用实际两轮日志离线重放，确认第一轮 7 个 reasoning 项和助手消息共 8 项全部恢复、所有原始字段一致；不修改真实日志与数据库。使用 gpt-6-astra max 进行裁剪首尾空白的三轮真实验证，第二轮输入包含前轮推理且缓存命中 3840 token；缓存命中仍取决于上游，不保证每轮命中。

### 普通聊天客户端的 Responses 推理状态恢复

- Responses 原生上游按已完成助手轮次的可见历史前缀保存和恢复完整原始输出，包括加密推理项、工具调用及阶段信息。手机客户端无需回传思维链、签名或 provider_metadata，末尾追加多条消息和连续多轮均可匹配。
- 使用已有会话数据库持久化压缩缓存，复用三天闲置过期策略；按调用者、模型、端点、所选凭据和有效提示模板隔离，显式会话 ID 进一步隔离会话。同一前缀出现不同原始输出时标记为歧义，不自动选择其中一个。
- 恢复在每次上游凭据选定后执行，重试换 Key 时从客户端历史重新构建请求；客户端显式提供的原始输出优先保留。
- 流式在完成事件交给客户端前保存原始输出；终态省略 output 时按 output_item.done 的索引补齐，不缓存失败、截断或未完成响应。
- 新增历史追加、工具轮次、历史修改/截断、隔离、歧义、过期、流式保存时序和换 Key 重试回归测试。使用 gpt-5.6-luna high 完成三轮真实桥接测试，确认流式与非流式逐字回传前轮密文。

## 2026-09-05

### 监控界面与请求详情优化

- 将监控样式移至独立 CSS，移除旧版强制覆盖，统一深色卡片、按钮、筛选器与表格排版。
- 请求详情采用分区布局，阶段耗时独立展示，上游用量、重试记录与价格快照支持折叠阅读，归档下载入口统一样式。
- 改善窄屏布局，固定详情标题与关闭按钮，支持 Escape 关闭、键盘焦点循环及关闭后恢复焦点。

### 统计接口回归修复

- 修复替换历史价格重算函数时误删三个异步统计方法的问题，恢复 Token 统计、请求统计和概览汇总的线程池查询入口。
- 新增真实 SQLite 管理路由测试，验证统计不会错误回退、参数完整传递、查询不阻塞事件循环，且历史金额不变。

### 网关策略实施、原生工具与监控

- 全部模型统一 Schema 保真，移除隐式递归清洗及 strict 降级；新增带备份、历史金额校验和幂等检查的策略迁移脚本。
- 使用共享执行器限制重试边界，首业务事件返回后不再重放；保持原 sticky 长期冷却规则，修复重复重试与流缺少终态的错误处理。
- 接入认证后的调用方上下文、三天闲置会话缓存和永久 gzip 原生交换归档；OpenRouter/Gemini 思考签名隔离并精确匹配，优先保留客户端字段。
- Gemini 原生路由使用共享转发器，增加原生 Interactions 入口；兼容 OpenAI、DeepSeek 官方 web_search、百炼 Qwen 和 Gemini 的原生工具及相关事件、引用、签名。
- 新增管理员 Playground、文档能力诊断、实际工具输出观察、请求中断、完整响应下载和阶段耗时；提供四项只读网关管理工具及 MCP Streamable HTTP。
- 新增调用方、会话、网关请求 ID、耗时和调用时价格快照，覆盖 SQLite 全部相关读写；历史金额不再启动重算，现价分析独立只读。
- Key 保存/重载失败明确报错，普通配置变更回滚，删除和禁用保持当前进程立即生效；自定义 tokenizer 的远程代码改为按来源开启。
- 图床保留三十分钟并限制上传大小；请求日志永久保留且新文件无损压缩，支持混合读取旧 JSON/gzip 日志。
- 监控可搜索调用方和会话，显示阶段耗时与价格快照；长文本点击时才渲染全文，详情异步响应按当前窗口过滤，活跃请求不重复重建相同 DOM。
- 验证：279 项测试通过，128 个 Python 文件、17 个 JavaScript 文件语法检查通过；隔离环境验证应用装配，真实数据库迁移确认历史金额不变。供应商调用使用模拟上游。

### 全项目审查与兼容性修复

- 修复未配置 API Key 时携带任意 Key 可绕过本机限制；补齐 Gemini 模型列表鉴权方式与访客白名单过滤，非法请求类型返回 400、未知模型返回 404。
- 补齐模型删除/排序和 JSONC 管理写入的事务锁；无效配置保留最后有效状态；串行化 Key 管理与重载，防止较早快照恢复已撤销 Key。
- 修复配置模式切换丢失修改、注释丢失、字符串解析错误、有效 0 被默认值覆盖和浅拷贝污染；前后端及独立图床统一支持字符串感知的 JSONC 解析。
- 模型编辑保留其他端点和未在表单展示的配置字段；编辑保存始终检查原模型存在。未带内容指纹的 JS/CSS 重新验证浏览器缓存。
- 监控日志只接受最新查询结果，标签切换不依赖全局 event，费用属性统一转义；CSV 增加真实 UTF-8 BOM、防公式文本处理和正确的币种列名。
- 修复模型历史 USD/CNY 记录直接相加的问题，按当前汇率转换后汇总；自动归档统计查询移到线程池。
- 提取 SSE JSON 解析器，支持多行 data、UTF-8/CRLF 分块和最后无空行事件，避免大事件反复复制累计字符串。
- Gemini GenerateContent 补齐工具签名回传、跨分块调用编号、文本与工具共存、完成原因、JSON Schema 与停止序列转换。
- Anthropic 双向转换增加 strict、并行工具控制、结构化输出和 effort 映射；思考配置只修改 output_config.effort，不再删除输出格式。
- Responses 原生请求使用独立副本；流缺少终态事件时报告失败，content_filter 在转换分支保持 incomplete 状态。
- 修复独立图床的 HTTP 错误状态、base64 校验、清除元数据时丢失格式、图片处理阻塞事件循环，以及独立依赖缺少 Pillow。
- 补录工作区已有的 schema_sanitizer 依赖，修正 README 初始化示例与结构说明，增加开发测试依赖。
- 增加全项目审查报告，核对 11 家供应商文档，集中记录待决定的兼容策略、重试、权限和功能扩展；提供未注册到网关的只读 LLM 工具样例。
- 测试在收集模块前切换到独立临时目录，避免导入网关模块时初始化业务目录；真实页面资产仍从仓库读取。
- 验证：隔离环境下 244 个测试通过；105 个 Python 文件、16 个 JavaScript 文件和 2 段内联脚本语法检查通过。供应商调用使用模拟传输，没有进行付费线上探测。

### 管理界面重构

- 调整管理面板的导航、留白、按钮、卡片和移动端布局；按使用偏好采用直角控件，增加侧栏宽度与间距，避免导航文字换行。
- 模型列表增加名称 / 模型 ID / 地址搜索、接口协议筛选、服务地址筛选、数量提示和无结果状态。设置入口常驻，复制、归档和删除放入更多操作。
- 批量全选只作用于筛选后可见的模型，显示所选数量和部分选中状态。筛选期间暂停拖动排序，防止误改隐藏模型的顺序。
- 模型编辑器改为六组导航：连接与身份、生成与思考、消息与兼容、重试与图片、计费与统计、自定义参数。固定保存栏，支持未保存提醒、关闭后恢复焦点、键盘导航及 Ctrl / Cmd + S。
- 保存期间防止重复提交；失败保留输入并允许重试。新增 / 复制时检查当前列表中的同名模型，避免意外覆盖。
- API Key 默认遮蔽，可逐行显示；列表不再将含 Key 的完整配置嵌入 HTML 属性。
- 将模型编辑代码按表单保存、选项配置、Key 管理和界面交互拆分，保留原有配置格式及 LMArena 兼容入口。

### 修复

- 重置表单时恢复初始值，避免新增模型沿用之前编辑的兼容开关、计费等状态。
- 保留价格、缓存价格、温度上限中的有效数值 0。
- 根据接口协议显示默认端点；切换协议时保留自定义端点，并只显示适用的透传选项。
- 修复空 Key 行导致余额查询结果与输入行错位，以及余额货币字段缺少 HTML 转义的问题。
- 修复会话失效消息参数顺序、归档失败产生未处理 Promise，以及并发刷新模型列表时旧响应覆盖新响应的问题。

### 验证

- 新增真实 Chromium 浏览器回归，使用隔离模拟 API，不读写真实模型配置或调用上游。
- 浏览器测试：`python -m pytest tests/test_admin_workspace.py -q`。依赖 `pytest`、`playwright`、`beautifulsoup4`，浏览器通过 `python -m playwright install chromium` 安装。
- 模型归档回归：`python -m pytest tests/test_model_archive.py -q`。
